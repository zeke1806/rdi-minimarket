overwrite: true
schema: 'http://localhost:3000/graphql'
documents: null
generates:
    src/api/types.ts:
        plugins:
            - 'typescript'
            - 'typescript-operations'
            - 'typescript-react-apollo'
    ./graphql.schema.json:
        plugins:
            - 'introspection'
